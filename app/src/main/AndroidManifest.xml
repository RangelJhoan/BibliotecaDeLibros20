<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bibliotecadelibros20">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BibliotecaDeLibros20">
        <activity
            android:name=".view.activities.registro.RegistrarseActivity"
            android:exported="true" />
        <activity
            android:name=".view.activities.AdministradorActivity"
            android:exported="true"/>
        <activity
            android:name=".view.activities.UsuarioActivity"
            android:exported="true" />
        <activity
            android:name=".view.activities.Login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>